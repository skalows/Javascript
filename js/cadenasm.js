function extraer() {
var nombre = "roberto carlos";
var nuevotexto = nombre.substr(3);
nuevotexto = nuevotexto.toUpperCase();
//toLowerCase(); pasa todo el texto a minusculas
//textOfIndex
//replace() remplaza una parte de la cadena por lo que se ingrese como parametro
//
document.write(nuevotexto);
}
extraer();
